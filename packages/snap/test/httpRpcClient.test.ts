import { BigNumber } from 'ethers';

describe('client module - HttpRpcClient', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('getEntryPointAddr', () => {
    it('placeholder', async () => {
      expect(BigNumber.from(1).toString()).toBe(BigNumber.from(1).toString());
    });
  });

  describe('getAccountFactoryAddr', () => {
    it('placeholder', async () => {
      expect(true).toBe(true);
    });
  });

  describe('getEntryPointContract', () => {
    it('placeholder', async () => {
      expect(true).toBe(true);
    });
  });

  describe('getBundlerUrl', () => {
    it('placeholder', async () => {
      expect(true).toBe(true);
    });
  });

  describe('send', () => {
    it('placeholder', async () => {
      expect(true).toBe(true);
    });
  });
});
