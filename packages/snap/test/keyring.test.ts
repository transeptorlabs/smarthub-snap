import { BigNumber } from 'ethers';

describe('Keyring module', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('getSimpleScAccount', () => {
    it('placeholder', async () => {
      expect(BigNumber.from(1).toString()).toBe(BigNumber.from(1).toString());
    });
  });
});
